package com.example.matchmate.model

/**
 * Data class to hold the coordinates of the profile.
 * @property latitude the latitude coordinate of the profile.
 * @property longitude the longitude coordinate of the profile.
 */
data class Coordinates(
    val latitude: String,
    val longitude: String
)