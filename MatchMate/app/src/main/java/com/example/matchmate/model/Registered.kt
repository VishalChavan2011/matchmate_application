package com.example.matchmate.model

/**
 * Data class to hold the Registered time of the profile.
 * @property age the age when registered.
 * @property date the date when registered.
 */
data class Registered(
    val age: Int,
    val date: String
)