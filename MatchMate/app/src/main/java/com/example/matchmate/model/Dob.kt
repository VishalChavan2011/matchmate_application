package com.example.matchmate.model

/**
 * Data class to hold the Date of birth of the profile.
 * @property age the age of the profile.
 * @property date date of birth of the profile.
 */
data class Dob(
    val age: Int,
    val date: String
)