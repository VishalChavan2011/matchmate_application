package com.example.matchmate.model

/**
 * Data class to hold the Name of the profile.
 * @property first the first name.
 * @property last the last name.
 * @property title the title of the profile.
 */
data class Name(
    val first: String,
    val last: String,
    val title: String
)